function saludar() {           //Creamos una funcion con nombre saludar.
    console.log("Esto es una funcion");

}

saludar();  //llamamos a la funcion de esta manera.


function suma() {  //sumar dos numeros.
    return 2 + 2;
}

let resultado = suma() //usamos una variable para mostrar el resultado de la funcion suma.
console.log(resultado);

//let resultado = suma() //usamos una variable para mostrar el resultado de la funcion suma.
console.log(suma()); //otra forma de llamar la funcion.
